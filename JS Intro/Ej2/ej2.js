"use strict"
var alumno ={ // var alumno  se define con = y no con :
    nombre : "Juan", //Se usa , simple al enumerarlo
    apellidos : "González", //Se usa : en vez de = similar a un json
    notas : [8,8,2,4], //Se usa [] en vez de () para arrays
    dni : "4098976",
    edad : '18'
    };

console.log(alumno);